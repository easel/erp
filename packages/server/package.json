{
  "name": "@saterp/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "migrate": "graphile-migrate migrate"
  },
  "dependencies": {
    "@saterp/shared": "workspace:*"
  }
}
