apiVersion: v1
kind: Service
metadata:
  name: {{ include "apogee.fullname" . }}
  labels:
    {{- include "apogee.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apogee.selectorLabels" . | nindent 4 }}
